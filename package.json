{
  "name": "corthography-client-monorepo",
  "version": "0.1.2",
  "private": true,
  "description": "TypeScript SDK, CLI, and Claude plugin for Corthography Press",
  "license": "MIT",
  "workspaces": [
    "sdk/js",
    "cli"
  ],
  "scripts": {
    "build": "npm run build -w @corthos/corthography-sdk && npm run build -w @corthos/corthography-cli",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
