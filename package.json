{
  "name": "corthography-client-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript SDK, CLI, and Claude plugin for Corthography Press",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @corthography/client && npm run build -w @corthography/cli",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
